# AI Health Insight Companion — Python Dependencies
# Install with: pip install -r requirements.txt

# -- FastAPI stack --
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# -- PDF extraction --
pdfplumber>=0.11.0

# -- Data validation --
pydantic>=2.7.0

# -- LangChain core --
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# -- LLM providers --
langchain-groq>=0.2.0

# -- Vector store (FAISS with mock embeddings — no OpenAI key needed) --
faiss-cpu>=1.8.0

# -- Utilities --
python-dotenv>=1.0.0        # load GROQ_API_KEY from .env file
