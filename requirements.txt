# AI Health Insight Companion — Python Dependencies
# Install with: pip install -r requirements.txt

# LangChain core
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# Groq LLM (primary inference engine)
langchain-groq>=0.2.0

# FAISS — vector store for RAG (mock embeddings, no OpenAI key needed)
faiss-cpu>=1.8.0

# Utility
python-dotenv>=1.0.0        # load GROQ_API_KEY from .env file
